#include <stdio.h>
#include "main.h"

/**
 * mul - checks the code to multiply two integers
 * @a: is the integer variable involved in the multiplication
 * @b: is the secont integer variable involved in the multiplication
 * Return: Result
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
